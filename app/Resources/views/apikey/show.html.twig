{% extends 'base.html.twig' %}

{% block body %}
    <h1>Apikey</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ apiKey.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ apiKey.name }}</td>
            </tr>
            <tr>
                <th>Keycode</th>
                <td>{{ apiKey.keyCode }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('apikey_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('apikey_edit', { 'id': apiKey.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
